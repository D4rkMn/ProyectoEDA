#include "Metric/Sil/Sil.h"

template<typename T>
float Sil<T>::execute(std::vector<T>& R, DataSet<T>& O, std::vector<Cluster<T>>& C) {
    // TODO: Implementar el índice Silhouette*
    return 0.0f;
}