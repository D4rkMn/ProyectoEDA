#include "Metric/DB/DB.h"

template<typename T>
float DB<T>::execute(std::vector<T>& R, DataSet<T>& O, std::vector<Cluster<T>>& C) {
    // TODO: Implementar el índice Davies-Bouldin*
    return 0.0f;
}