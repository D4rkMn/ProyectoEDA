#include "Algorithms/BRID/BRID.h"

template<typename T>
std::vector<T> BRID<T>::execute(size_t k, const T& o_q, DataSet<T>& O, std::vector<Cluster<T>>& C) {
    // TODO: Implementar el algoritmo BRID
    return std::vector<T>();
}