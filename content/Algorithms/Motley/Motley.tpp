#include "Algorithms/Motley/Motley.h"

template<typename T>
std::vector<T> Motley<T>::execute(size_t k, const T& o_q, DataSet<T>& O, std::vector<Cluster<T>>& C) {
    // TODO: Implementar el algoritmo Motley
    return std::vector<T>();
}